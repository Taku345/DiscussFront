import React from 'react'
import RoomBoard from './RoomBoard';
import { Box } from '@mui/material';

const Home: React.FC = () => {

  return (
    <Box sx={{}}>
      <RoomBoard />
    </Box>
  )
}

export default Home;